

### JSON parse Error 

```bash
JSON parse error: Cannot deserialize value of type `com.clovircm.web.restapi.vm.optimization.VmOptimizationSearchConditionDTO` from Array value (token `JsonToken.START_ARRAY`)
```

- Controller 에서 객체타입의 변수를 받을 때 List 형태의 변수를 받아서 생기는 에러이다.
- DB Setting 
